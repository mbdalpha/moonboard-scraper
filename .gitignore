# --- secrets / credentials (NEVER commit) ---
credentials.json
data/auth_headers.json
data/captured_requests.jsonl
data/moon-wg.conf
data/moon-wg-qr.png

# logged-in browser session
browser-profile/

# captured traffic + debug dumps (tied to your account)
data/*.log
data/login_debug.html
data/dashboard_rendered.html
data/dashboard.png
data/page_*.html
data/helpers.min.js

# --- generated datasets (large; Moon Climbing's data — pull your own) ---
# names derive from config.json (board name + angle), e.g. moonboard2024_40.json
data/*.json
data_test/

# hold photos + layouts pulled from moonboard.com (Moon's imagery — pull your own)
data/holds/
data/moonlink_holds.zip

# python
__pycache__/
*.pyc
